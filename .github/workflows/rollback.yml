name: Rollback Workflow

on:
  workflow_run:
    workflows: ["Deployment Workflow"]
    types: [completed]

jobs:
  rollback:
    if: ${{ github.event.workflow_run.conclusion == 'failure' }}
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}

    steps:
      - name: Download environment for this failed run
        uses: actions/download-artifact@v4
        with:
          name: deployment-info-${{ github.event.workflow_run.id }}

      - name: Read environment value
        run: |
          ENVIRONMENT=$(cat environment.txt)
          echo "ENVIRONMENT=$ENVIRONMENT" >> $GITHUB_ENV
          echo "✔ Detected correct environment from failed deployment: $ENVIRONMENT"

      - name: Select rollback release
        run: |
          if [ "$ENVIRONMENT" = "staging" ]; then
            TAG=$(gh api repos/${{ github.repository }}/releases \
              --jq 'map(select(.prerelease == true)) | .[1].tag_name')
          else
            TAG=$(gh api repos/${{ github.repository }}/releases \
              --jq 'map(select(.prerelease == false)) | .[1].tag_name')
          fi
          
          echo "ROLLBACK_TAG=$TAG" >> $GITHUB_ENV
          echo "✔ Selected rollback tag: $TAG"

      - name: Checkout previous release
        uses: actions/checkout@v3
        with:
          ref: ${{ env.ROLLBACK_TAG }}

      - name: Perform rollback deployment
        run: |
          echo "🚨 Rolling back deployment using tag: $ROLLBACK_TAG"
          echo "🚀 Rollback completed successfully!"
